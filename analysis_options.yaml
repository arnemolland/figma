include: package:lints/recommended.yaml

analyzer:
  exclude: ['**/*.g.dart']

linter:
  rules:
    - directives_ordering
    - prefer_expression_function_bodies
    - unnecessary_await_in_return
    - unnecessary_lambdas
    - cascade_invocations