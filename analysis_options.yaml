include: package:lints/recommended.yaml

analyzer:
  exclude: ['**/*.g.dart']

linter:
  rules:
    - prefer_expression_function_bodies
    - prefer_final_locals
    - unnecessary_await_in_return
    - unnecessary_lambdas
